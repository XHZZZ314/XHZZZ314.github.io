# ChoreoHelper — FF14 国服编舞助手

时间轴编舞 · 游戏内实时播放 · 拖动定格预览 · 宏分页生成

**网页应用：https://xhzzz314.github.io/choreo/**
**安装指南：https://xhzzz314.github.io/choreo/install.html**

## 功能

- **时间轴编辑**：多舞者轨道、动作/切换/台词三种片段、拖拽调整、冲突自动顺延、空隙自动补齐
- **动作库**：全部游戏表情 + 职业技能（自动插入职业切换），显示动作实测时长
- **拖动定格预览**：在时间轴上来回拖动，游戏角色实时显示对应时刻的姿势（Ktisis 同款直接时间轴技术）
- **游戏内播放**：整个编排按时间轴在游戏里实时播放
- **宏生成**：自动 /nextmacro 分页（每页 14 行）、一键复制、TXT 导出、歌词联动

## 安装（两种方式）

**方式一 · 插件仓库（推荐）**
1. 游戏内 `/xlplugins` → 设置 → 自定义插件仓库
2. 添加：`https://xhzzz314.github.io/choreo/repo/pluginmaster.json`
3. 搜索 ChoreoHelper 安装

**方式二 · 手动**
1. 下载 [latest.zip](./repo/latest.zip)
2. 关闭游戏，把 `ChoreoHelper.dll` 解压到 `%AppData%\XIVLauncherCN\devPlugins\ChoreoHelper\`
3. 启动游戏

装好插件后打开上面的网页应用即可（网页与游戏须在同一台电脑，通过 localhost:48794 通信）。

## 系统要求

- 最终幻想14 国服 + 橙月启动器（XIVLauncher CN，Dalamud API Level 15）

## 源码

`src/` 目录（C# / .NET 10，Dalamud 插件 + 单文件 HTML 前端）。

## 更新日志

- **v0.5.22**（2026-08-22）：修复 v0.5.21 引入的真实使用回退 —— 切换时的预防性 bow 打断条件过宽（表外普通动画/循环型待机变体都触发 = 用户实测"每次切换都插一个鞠躬、错误特别多"）。改为卡住才打断：仅在重试时刻确认绑不上且槽上是表外循环时间轴（舞蹈/打盹类残留、非待机）才 bow；打断器排除待机集合。真实节奏验证：0 次误打断、33/40 命中（冷缓存含学习）、黄金之舞/挥手场景秒钉；混沌压测 400 跳后排空复检仅剩游戏侧锁死类
- **v0.5.21**（2026-08-22）：多轮超高压测试（6 轮 × ~3000 次拖动：快速扫掠/随机混沌/同轨刮擦/空隙启停/播放交错/5 分钟马拉松）暴露的极限场景加固——挂载等待期也每帧强制 BaseOverride（此前只在挂载后守，等待期被上一轮残留动画顶掉 = "钉住但绑不上"）；pin-learned 通道绑定在途时不再急发重试（1.6s 耐心窗），卡住时解钉走表情通道强制换绑；换轮后旧钉住绝不再重钉（防播出上一命令的动画）；重钉校验归属；版本横幅修正。压测结论：0 HTTP 错误、0 崩溃、0 学习污染（1:1 无共享），风暴后静止复检 98.3% 命中（余为面部/游戏侧锁死/在途积压瞬态）
- **v0.5.20**（2026-08-22）：全量动作验证修复——有学习缓存时切换不再放 bow 打断（曾与瞬时钉住竞争，把刚钉上的动画顶掉 = 可用动作偶发不生效，如 /flamedance）；新增"执行后 EmoteId 状态转移确认"学习路径（/gcsalute 等解析到族外变体行的命令此前永远学不到 = 永不挂载）；时间写入统一按活体绑定时长取模并每帧对账（修 2.8%2.33=0.47 这类错位时间）。全量套件：293 动作顺序跳 + 每个正反拖 3 次不同位置，225 个学习挂载、31 面部类、37 个游戏侧锁死（角色未解锁，聊天命令也只播共享回退动画），全程 0 交叉污染、0 命令错位
- **v0.5.19**（2026-08-22）：动画钉住架构重做（参考 Brio/Ktisis 实现）——挂载确认后用 BaseOverride+AnimLock 接管基础槽，游戏的表情结束计时器/姿态切换再也收不走动作（黄金之舞半程失效根治）；模式切换全部走 SetMode 函数（裸写 Mode 曾让表情系统静默拒绝后续一切命令 = 跳一次舞后全部失效的死锁）；游戏变体解析行并集 + 学习独占所有权（在途 motion 命令延迟落地曾把霹雳舞变体学给害怕 = 拖动播出错误动作，根治）；循环变体 bow 打断；表情分类按行判定（挥手中断修复）；停止竞态世代守卫；黄金之舞 20 秒长定格 + 560 动作来回拖动压测零错误
- **v0.5.16**（2026-08-22）：修复命令风暴（有界重试，每次结算最多 3 条命令，删除无限 motion 兜底——消除"没写过的动作"乱入）；姿态锁定解除（坐地/躺椅后的站姿命令被游戏拒绝 = 很多动作出错，现强制回主待机立即解除）；待机集合预置
- **v0.5.15**（2026-08-22）：特殊动作修复——执行通道全部由游戏解析变体；面部表情类免定格
- **v0.5.14**（2026-08-22）：恢复集合语义挂载验证——游戏会把强制时间轴 id 规范化成它自己解析的变体，精确匹配会误判失败导致反复重放（抽搐）与错误变体闪烁（动作错）；播放候选限制为学习过的 id + 主 id
- **v0.5.13**（2026-08-22）：删除时长匹配挂载与全族轮换
- **v0.5.12**（2026-08-22）：动画执行通道重构——直接强制时间轴（瞬时、无忙窗口）；拖动流分离（拖动中不执行、停下精准定格）；三层停表 + Sequencer 槽速度冻结；变体学习缓存 + 族内 id 轮换；循环动画取模；560 动作压测零错误
- **v0.5.2**（2026-08-21）：首个发布版本
